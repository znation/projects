#include <stdbool.h>
#include <glib.h>

int max(int x, int y);
GList *digits(int x);
int undigits(GList *digits);
int ipow(int x, int y);
int isqrt(int x);
bool prime(int x);
GList *listOfPrimes();
bool pandigital9(int x);

